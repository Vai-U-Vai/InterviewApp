<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/surface" /> <!-- Фон -->
    <corners android:radius="8dp" /> <!-- Скруглённые углы -->
    <stroke
        android:width="1dp"
        android:color="@color/secondary_text" /> <!-- Обводка -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" /> <!-- Внутренние отступы -->
</shape>
